services:
  app:
    build: .
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      # only logs need to persist — inventory.db is created fresh at build time via setup_db.py
      - ./logs:/app/logs
